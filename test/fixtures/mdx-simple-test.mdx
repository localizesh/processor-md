<div>
  Here’s an expression:
</div>

<table>
  <thead>
    <tr>
      <th>Rule</th>
      <th>Rationale</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>touch-action</code> is prohibited.</td>
      <td>An ad that can manipulate <code>touch-action</code> can interfere with
        the user's ability to scroll the host document.</td>
    </tr>
  </tbody>
</table>

import Box from "place";

{
  1 + 1 /* } */
}

Which you can also put inline: {1+1}. {2+3}

<Box>
  - Lists, which can be indented.
</Box>

<Box>
  - Lists, which can be indented.
</Box>

<Box title={"title"} props={"test"}>
  <Box title={"title"} props={"test"}>
    <Box title={"title"} props={"test"}>
      <Box title={"title"} props={"test"}>
        <Box>
          - Lists, which can be indented.
        </Box>
      </Box>
    </Box>
  </Box>
</Box>

Which you can also put inline

<Box title={"title"} props={"test"}>
  <Box>
    - Lists, which can be indented.
  </Box>
</Box>

Which you can also put inline

<Spinner title={"title"} props={"test"}>
  <Spinner>
    - Lists, which can be indented.
  </Spinner>
</Spinner>

Which you can also put inline

export const info = {
  author: [
    {name: 'Titus Wormer', github: 'wooorm', twitter: 'wooorm'}
  ],
  published: new Date('2021-10-06'),
  modified: new Date('2023-01-19')
}

await compile(file, {remarkPlugins: [remarkMath], rehypePlugins: [rehypeKatex]})

export default class AddImageButton extends Component {
  onInputClick = (event) => {
    event.target.value = "";
  };

  onLabelClick = () => {
  this.link.blur();
  };

  render() {
  }
}

